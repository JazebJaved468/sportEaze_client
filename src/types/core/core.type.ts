export type ToastType = {
  message: string;
  isVisible: boolean;
};

export type SearchedUser = {
  id: string;
  profilePicUrl: string;
  fullName: string;
  username: string;
  userType: number;
};
