export enum USER_TYPE {
  FAN = 0,
  PLAYER = 1,
  PATRON = 2,
  MENTOR = 3,
}

export enum GENDER {
  Male = 0,
  Female = 1,
  Other = 2,
}

export enum MediaType {
  IMAGE = 1,
  VIDEO = 2,
  DOCUMENT = 3,
}
