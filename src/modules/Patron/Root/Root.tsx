import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {PatronBottomTabNavigator} from '../../Core/Navigator/BottomTabNavigator/BottomTabNavigator';

export const PatronRoot = () => {
  return <PatronBottomTabNavigator />;
};

const styles = StyleSheet.create({});
