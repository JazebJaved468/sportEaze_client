import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {FanBottomTabNavigator} from '../../Core/Navigator/BottomTabNavigator/BottomTabNavigator';

export const FanRoot = () => {
  return <FanBottomTabNavigator />;
};

const styles = StyleSheet.create({});
